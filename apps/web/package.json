{
  "name": "@yalisp/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build-seed": "node scripts/build-seed.mjs && node scripts/build-aot.mjs",
    "prebuild": "npm run build-seed",
    "predev": "npm run build-seed",
    "pretest": "npm run build-seed",
    "build": "vite build && node scripts/prune-mounted-assets.mjs",
    "dev": "vite --host 0.0.0.0",
    "preview": "vite preview --host 0.0.0.0",
    "example:hello": "node examples/hello-world/cli.mjs",
    "measure:wolf3d-feasibility": "node scripts/measure-wolf3d-feasibility.mjs",
    "mount:assets": "node scripts/mount-assets.mjs",
    "test": "node --test tests/*.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@yalisp/site-content": "*"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "wabt": "^1.0.39"
  }
}
